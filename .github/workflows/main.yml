name: CI

on: 
  push:
    branch: main

  workflow_dispatch:
  
jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v3
      - name: run one line script
        run: echo Hello World!)
